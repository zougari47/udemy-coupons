---
import { cn } from '@/lib/utils';

interface Props {
  class?: string;
}

const { class: className, ...props } = Astro.props;
---

<li data-slot='pagination-item' class={cn(className)} {...props}>
  <slot />
</li>
